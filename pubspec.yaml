name: cross_file_image
description: ImageProvider for XFile.
version: 1.0.2
homepage: https://github.com/WatchDogsDev/cross_file_image

environment:
  sdk: ">=2.14.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  cross_file: ^0.3.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
